# Installed on Render during build: pip install -r requirements.txt
# Use a Render PostgreSQL instance and set DATABASE_URL (see production settings).

Django>=4.2,<5.1
djangorestframework>=3.15.0
djangorestframework-simplejwt>=5.3.0

# PostgreSQL (Render DATABASE_URL + django.db.backends.postgresql)
psycopg2-binary>=2.9.9

python-decouple>=3.8
dj-database-url>=2.1.0

openai>=1.0.0

django-cors-headers>=4.3.0
Pillow>=10.0.0
django-ratelimit>=4.1.0

gunicorn>=21.0.0
whitenoise>=6.6.0
